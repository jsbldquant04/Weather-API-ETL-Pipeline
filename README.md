# Weather-API-ETL-Pipeline
An end-to-end ETL pipeline that extracts weather data from the Open-Meteo API, transforms and validates the data using Python/Pandas, stores the results in SQLite, and produces an interactive weather dashboard using Plotly.
